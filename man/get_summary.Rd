% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_summary.R
\name{get_summary}
\alias{get_summary}
\title{Retrieve Historical Aggregate Data by Year for FDIC-Insured Institutions}
\usage{
get_summary(
  api_key = Sys.getenv("FDIC_API_KEY"),
  filters = NULL,
  fields = NULL,
  sort_by = NULL,
  descending = FALSE,
  limit = 10000
)
}
\arguments{
\item{api_key}{(String) Your FDIC API key. Defaults to the value of the
\code{FDIC_API_KEY} environment variable.}

\item{filters}{(String) An optional Elasticsearch query string to filter
results. All field names and values must be uppercase.}

\item{fields}{(String or Character vector) Fields to include in the response.
An \code{ID} column is always present, regardless of the fields requested.
To retrieve the most recently published API definition for an endpoint,
replace the \code{get_} prefix in the function name with \code{fdic_}
(e.g., \verb{get_\{endpoint\}()} to \verb{fdic_\{endpoint\}()}).}

\item{sort_by}{(String) Field name to sort results by. Defaults to the API
default sort order for this endpoint.}

\item{descending}{(Logical) Should results be sorted in descending order?
Only applies when \code{sort_by} is specified. Defaults to \code{FALSE}.}

\item{limit}{(Integer) Number of records to return. Must be between 1 and
10,000. Defaults to 10,000.}
}
\value{
A tibble containing historic aggregate financial
and structure data regarding financial institutions, with one row per
quarter and year.
}
\description{
Queries the \verb{/summary} endpoint of the FDIC BankFind Suite API,
returning aggregate data by bank type, state, quarter, and year
for FDIC-insured financial institutions.
}
\examples{
\dontshow{if (!no_creds_available()) withAutoprint(\{ # examplesIf}
# Return summary data for each bank type, state, quarter, and year
get_summary(
  fields = c("ASSET", "BANKS", "BRANCHES", "CALLYM", "NETINC"),
  limit = 5
)

# Return summary data sorted by reporting period descending
get_summary(
  fields = c("ASSET", "BANKS", "CALLYM", "YEAR"),
  sort_by = "CALLYM",
  descending = TRUE,
  limit = 5
)
\dontshow{\}) # examplesIf}
}
